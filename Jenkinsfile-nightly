pipeline {
    agent any

    stages {

        stage ('Nightly Docker Build') {
            steps {
                sh 'docker build -f ci/Dockerfile -t my-playwright .'
            }
        }
    }
}